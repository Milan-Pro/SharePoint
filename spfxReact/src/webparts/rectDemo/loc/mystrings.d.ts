declare interface IRectDemoWebPartStrings {
  PropertyPaneDescription: string;
  BasicGroupName: string;
  DescriptionFieldLabel: string;
}

declare module 'RectDemoWebPartStrings' {
  const strings: IRectDemoWebPartStrings;
  export = strings;
}
